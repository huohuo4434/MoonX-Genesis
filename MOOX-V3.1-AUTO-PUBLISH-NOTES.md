# MOOX V3.1 自动发布连续性修复

本补丁基于已经安装的 `MOOX V3 autonomous upgrade`，解决本地验收时发现的真实问题。

## 已修复

1. 自动任务同时生成“本交易日”和“下一交易日”预测，不再只生成明日而让今日为空。
2. 取消明日观点的北京时间20:00展示门槛。系统生成后立即对有效会员可见。
3. 管理员不再是发布依赖；管理员覆盖只用于支撑压力、方向或突发事件修正。
4. 技术行情源暂时失败时，方向和路径仍然发布；支撑压力栏暂时隐藏，后续任务继续重试。
5. 明日页面不再因为缺少支撑压力而把整份预测隐藏。
6. 当前周度来源暂时断档时，最多沿用最近一周背景7天，明确标注“过渡预测、低置信度”，新周研究生成后自动替换。
7. 历史准确率页新增独立的周度验证区，日度与周度不混算。
8. 周度准确率使用：`(完全命中 + 部分命中 × 0.5) ÷ 有效样本`。无法验证不进入分母。
9. 76%仅作为长期目标，不修改真实历史结果。
10. 干支日期由确定性历法函数转换为公历日期，不允许AI凭记忆猜日期。
11. 修正历法测试：以 `2026-07-30 = 乙巳日` 为基准，2026-08-01是丁未日。因此在2026-08-01至08-16查找亥、卯、未日，正确日期为：08-01、08-05、08-09、08-13。
12. 去除开发环境对Google字体的联网依赖，避免本地Turbopack因无法连接 fonts.gstatic.com 而500。
13. 包含此前本地构建时手工修复的 Prisma nullable、未使用变量和参数类型问题。

## 权限没有放开

- 未登录用户：不返回预测正文。
- 普通注册用户：北京时间08:00后可查看今日观点。
- 有效会员：全天查看今日、明日和本周完整观点。
- 管理员：全部权限和可选覆盖入口。

自动发布不等于公开泄露会员内容。

## 自动验证时间

Vercel Cron使用UTC：

- 北京时间每日00:15：先验证BTC、A股、港股等已结束市场。
- 北京时间每日05:10：再次验证刚结束的美股和WTI，避免延迟到第二天。
- 周度最终验证：周一北京时间00:15，验证上一完整自然周。

现有周度数据区间为周一至周日，因此周日00:00时周日尚未走完，不能冒充“完整周验证”。

## 覆盖后执行

```powershell
npm install
npx prisma generate
npm run build
npm run dev
```

本地打开：

```text
http://localhost:3000
http://localhost:3000/verification
```

手动触发自动生成：

```text
http://localhost:3000/api/cron/generate-daily-forecasts
```

开发环境没有设置 `CRON_SECRET` 时可直接测试；生产环境由Vercel Cron携带密钥调用。

## 验收标准

1. 自动生成接口的 `records` 同时包含本日和下一交易日日期。
2. 技术点位抓取失败只进入 `warnings`，不能让该资产从 `records` 消失。
3. 未登录访问 `/api/forecasts/today` 仍返回 `LOGIN_REQUIRED`，但 `teaser.published` 和 `marketCount` 可用于确认系统已经生成，正文仍为空。
4. 有效会员登录后：首页今日、明日、本周均有内容。
5. 首页不再出现“每天20:00准时发布”或倒计时。
6. `/verification` 首屏显示周度验证区，下面保留日度验证详情。
7. 本周关键日期仅显示准确公历日期；没有可靠来源时不编造日期。
