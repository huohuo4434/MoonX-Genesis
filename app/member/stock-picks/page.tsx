import type { Metadata } from "next";
import Link from "next/link";
import { unstable_noStore as noStore } from "next/cache";
import { Badge, Heading, Section, Text } from "@/components/ui";
import { PublicFeaturePreview } from "@/components/access/PublicFeaturePreview";
import { MemberDeviceGate } from "@/components/access/MemberDeviceGate";
import { MemberDeviceHeartbeat } from "@/components/access/MemberDeviceHeartbeat";
import { MemberStockResearchDashboard } from "@/components/conviction/MemberStockResearchDashboard";
import { getMemberDevicePageAccess } from "@/lib/auth/member-device-guard";
import { getConvictionListPagePayload } from "@/lib/data/conviction/access";
import { buildLocalizedPageMetadata, getRequestLocale } from "@/lib/i18n/server";

export const dynamic = "force-dynamic"; export const revalidate = 0; const path = "/member/stock-picks";
export async function generateMetadata(): Promise<Metadata> { const locale = await getRequestLocale(); return buildLocalizedPageMetadata({ locale, basePath: path, titleZh: "会员股票研究 | MOOX", titleEn: "Member Stock Research | MOOX", descriptionZh: "从年度路线、整月阶段、本周六爻到日内奇门与缠论交叉验证。", descriptionEn: "Member research from yearly and monthly context to weekly Liu Yao and daily cross-checks." }); }
export default async function MemberStockPicksPage(){noStore();const gate=await getMemberDevicePageAccess();if(gate.status==="LOGIN_REQUIRED"||gate.status==="MEMBERSHIP_REQUIRED"){const en=(await getRequestLocale())==="en";return <main><Section spacing="lg"><PublicFeaturePreview eyebrow={en?"Stock research · Public preview":"股票研究 · 公开预览"} title={en?"Read the yearly regime before the weekly trade":"先看全年路线，再看本周阶段"} description={en?"Each asset aligns its yearly regime, monthly path, weekly route, projected candles and daily technical confirmation. Missing evidence remains blank.":"每个标的按年度路线、整月阶段、本周路径、模拟K线和日内技术确认排列；证据不足就留空。"} solves={en?["Locate the current stage","Compare year, month and week","Separate forecast candles from actual prices"]:["判断当前走到哪一段","对照年、月、周是否同向","区分预测K线与实际价格"]} memberBenefits={en?["Yearly, monthly and weekly route","Projected candles with date and price axes","4H structure and invalidation","Preserved verification history"]:["年、月、周路线","带日期和价格轴的模拟K线","4H结构与失效条件","保留历史验证结果"]} exampleTitle={en?"Stock research card":"股票研究卡"} exampleLines={en?["Year: rally then fade","Monthly: early rally","Weekly: range-up","Daily: wait for 4H confirmation"]:["全年：先涨后跌","整月：上行前段","本周：震荡上涨","日内：等待4H确认"]} nextPath={path} locale={en?"en":"zh"}/></Section></main>}if(gate.status==="DEVICE_REQUIRED")return <main><Section spacing="lg"><MemberDeviceGate decision={gate.device} nextPath={path}/></Section></main>;const payload=await getConvictionListPagePayload();return <><MemberDeviceHeartbeat/><main className="min-h-screen bg-[#07080a] text-white"><Section spacing="lg"><div className="mx-auto w-full max-w-[1240px]"><Badge variant="default">会员专享</Badge><Heading as="h1" size="h2" className="mt-4 text-white">股票研究 · 年月周日路线</Heading><Text variant="body" className="mt-3 block max-w-4xl text-white/58">先看全年月份候选与整月阶段，再看本周主走势；日分析并列周内节奏、奇门和4H缠论，跨周期分歧直接显示并降低信心。</Text><Link href="/member/sector-resonance" className="mt-6 flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-cyan-300/15 bg-cyan-300/[.04] px-5 py-4 text-sm text-cyan-100/75 transition hover:border-cyan-300/30 hover:bg-cyan-300/[.07]"><span><b className="text-white">查看板块共振分析</b><span className="ml-2 text-white/42">年度背景、本周与9月各周全部重点资产对照</span></span><span>进入 →</span></Link><div className="mt-7"><MemberStockResearchDashboard rows={payload.stockResearchRows ?? []}/></div></div></Section></main></>}
