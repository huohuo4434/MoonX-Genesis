/**
 * Chart and data-visualization primitives (Sparkline, MiniChart, etc.)
 * live here. Keep charting-library dependencies isolated to this folder
 * so they can be swapped without touching feature code.
 */
export {};
