/**
 * Domain-specific card components (e.g. ForecastCard, AssetCard) live here.
 * They compose `components/ui` primitives and stay presentation-only —
 * data fetching belongs in the page/route that renders them.
 */
export {};
